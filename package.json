{
  "name": "get-article-lambda",
  "packageManager": "yarn@4.10.3",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1"
  },
  "scripts": {
    "build": "sam build",
    "invoke:local": "yarn build && sam local invoke MyTypeScriptFunction -e ./mocks/event.json",
    "generate:mock": "sam local generate-event apigateway aws-proxy --method GET --path /get-article > ./mocks/event.json"
  }
}
